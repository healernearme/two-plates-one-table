/* Paste your deployed Google Apps Script Web App URL below to turn on
   cross-device syncing (see README.md → "Turn on syncing").
   Leave it as an empty string to run in local-only mode — the site still
   works fully, it just remembers choices on this device/browser only. */
const SYNC_URL = "";
