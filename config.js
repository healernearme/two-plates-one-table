/* Paste your Firebase Realtime Database URL below to turn on cross-device
   syncing (see README.md → "Turn on syncing") — something like
   "https://your-project-default-rtdb.firebaseio.com" (no trailing slash).
   Leave it as an empty string to run in local-only mode — the site still
   works fully, it just remembers choices on this device/browser only. */
const SYNC_URL = "";
